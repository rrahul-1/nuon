# Update

This is used to update the version of the `runner install` sytemctl service. It fetches the version info and ensures the
file is in place and executes a `systemctl restart`.

## Note

For now, we will also use this to restart the install runner.
