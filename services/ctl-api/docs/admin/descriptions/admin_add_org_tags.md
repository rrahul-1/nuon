Add one or more tags to an organization.

Tags are simple string labels used to categorize and organize organizations. Duplicate tags will be ignored - each tag can only exist once per organization.

Example tags: "enterprise", "priority", "beta-tester", "internal"
