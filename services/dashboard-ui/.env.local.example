# Example local environment variables for testing
# Copy this file to .env.local to enable local development features

# Auth0 Configuration (REQUIRED)
# Create a free Auth0 account at https://auth0.com and set up a new application
# to get these values. Configure your Auth0 application with:
# - Allowed Callback URLs: http://localhost:4000/api/auth/callback
# - Allowed Logout URLs: http://localhost:4000
# - Allowed Web Origins: http://localhost:4000
AUTH0_SECRET=your-random-secret-at-least-32-characters-long
AUTH0_BASE_URL=http://localhost:4000
AUTH0_ISSUER_BASE_URL=https://your-tenant.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_AUDIENCE=your-auth0-api-audience

# Feature Flags
# Enable full-screen onboarding instead of modal (set to 'true' to test)
NEXT_PUBLIC_ENABLE_FULL_SCREEN_ONBOARDING=false

# Auth Service Configuration (OPTIONAL)
# Uncomment NUON_AUTH_SERVICE_URL to use Nuon auth service instead of Auth0
# NUON_AUTH_SERVICE_URL=http://localhost:8084
NUON_APP_URL=http://localhost:4000
